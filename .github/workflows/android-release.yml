name: Android Release APK (cloud)

on:
  workflow_dispatch:

jobs:
  build-release:
    runs-on: ubuntu-latest
    env:
      EXPO_NO_DEV_CLIENT: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install deps
        run: npm ci --legacy-peer-deps

      - name: Create .env.production
        run: |
          echo "EXPO_PUBLIC_API_URL=${{ secrets.EXPO_PUBLIC_API_URL }}" >> .env.production
          echo "EXPO_PUBLIC_SUPABASE_URL=${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}" >> .env.production
          echo "EXPO_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}" >> .env.production
          echo "EXPO_PUBLIC_STREAM_ENDPOINT=${{ secrets.EXPO_PUBLIC_STREAM_ENDPOINT }}" >> .env.production
          echo "EXPO_PUBLIC_STREAM_CLIENT_NAME=${{ secrets.EXPO_PUBLIC_STREAM_CLIENT_NAME }}" >> .env.production
          echo "EXPO_PUBLIC_STREAM_CLIENT_VERSION=${{ secrets.EXPO_PUBLIC_STREAM_CLIENT_VERSION }}" >> .env.production
          echo "EXPO_PUBLIC_OFFLINE_USER_1=${{ secrets.EXPO_PUBLIC_OFFLINE_USER_1 }}" >> .env.production
          echo "EXPO_PUBLIC_OFFLINE_USER_2=${{ secrets.EXPO_PUBLIC_OFFLINE_USER_2 }}" >> .env.production

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Gradle permissions
        run: chmod +x android/gradlew

      - name: Create temp keystore (debug-style)
        working-directory: android
        run: |
          keytool -genkey -v -keystore debug.keystore \
            -storepass android -alias androiddebugkey -keypass android \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: Build RELEASE APK (signed)
        working-directory: android
        run: |
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$GITHUB_WORKSPACE/android/debug.keystore \
            -Pandroid.injected.signing.store.password=android \
            -Pandroid.injected.signing.key.alias=androiddebugkey \
            -Pandroid.injected.signing.key.password=android

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: beatly-beta-release
          path: android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error